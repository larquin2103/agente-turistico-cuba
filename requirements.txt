# Agente Turístico Cuba — Dependencias
# Instalar con: pip install -r requirements.txt

# API y servidor
fastapi>=0.100.0
uvicorn[standard]>=0.23.0

# Bot de Telegram (versión async ≥20)
python-telegram-bot>=20.6

# HTTP async
httpx>=0.25.0

# Variables de entorno
python-dotenv>=1.0.0

# Base de datos vectorial
chromadb>=0.4.0

# RAG — núcleo de LlamaIndex
llama-index-core>=0.10.0

# LLM — Groq (rápido, gratuito, sin GPU)
llama-index-llms-groq>=0.1.0
groq>=0.4.0

# Embeddings locales (sin API key)
llama-index-embeddings-fastembed>=0.1.0
fastembed>=0.2.0

# Vector store
llama-index-vector-stores-chroma>=0.1.0
